import AboutContent from "@/components/AboutContent";

export const metadata = {
  title: "About",
  description:
    "Why mycarbonfootprint.org exists: open data, real numbers, no tracking. Personal carbon footprint with sources you can audit.",
};

export default function AboutPage() {
  return <AboutContent />;
}
